# Global preferences

## Comments

Only write a comment when it carries non-obvious "why" information — rationale, precedent, a subtle invariant, a gotcha. Never write comments that restate "what" the code plainly does. When in doubt, prefer a clearer name or omit the comment entirely.
